#!/usr/bin/env bash
# gh-prs — list open PRs for a repo via gh-triage
set -euo pipefail

if [[ $# -lt 1 ]]; then
  echo "usage: $0 OWNER/REPO" >&2
  exit 1
fi

repo="$1"
skill_dir="$(cd "$(dirname "$0")/.." && pwd)"
cd "$skill_dir/../_shared/gh-triage"

uv run gh-triage prs "$repo"
